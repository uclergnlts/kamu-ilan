services:
  api:
    build: .
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: sqlite:////data/ilandetect.db
    ports:
      - "8000:8000"
    volumes:
      - ilandetect-data:/data
    restart: unless-stopped

volumes:
  ilandetect-data:

